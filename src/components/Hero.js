import React from 'react';
import { Container } from 'bootstrap-4-react/lib/components/layout';

const Hero = () => {
	return (
		<Container fluid className='hero'>
			<Container className='hero__intro'>
				<h1>How TaxPrepPro Works?</h1>
			</Container>
		</Container>
	);
};

export default Hero;
